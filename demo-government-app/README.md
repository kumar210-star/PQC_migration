# Citizen Secure Records

This fictional application contains synthetic configuration only. Its business operations call the crypto-agility interfaces in `backend/app/crypto_agility`; provider selection is controlled centrally through the API and does not require changing application code.

